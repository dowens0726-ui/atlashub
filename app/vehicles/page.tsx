import { AppShell } from "@/app/components/layout";
import VehicleWorkspace from "@/app/components/vehicles/VehicleWorkspace";

export default function VehiclesPage() {
  return (
    <AppShell>
      <VehicleWorkspace />
    </AppShell>
  );
}
